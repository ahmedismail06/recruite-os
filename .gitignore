node_modules/
.next/
.env
.env.local
*.log
.DS_Store
# LaTeX build artifacts
*.aux
*.out
*.fls
*.fdb_latexmk
*.synctex.gz
resumes/**/*.log

# Personal application content — never tracked in git
/knowledge-base/
/resumes/
/cover-letters/

# Per-user setup output — never tracked in git
/recruiting-os.config.json
/supabase/schema.sql
# Local-mode data store (your personal recruiting data). The template README is
# committed via a negated rule so the folder layout is discoverable on clone.
/data/*
!/data/README.md
